<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.henu.mapper.CategoryMapper">
    <resultMap id="categoryMap" type="com.henu.entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="remark" property="remark"/>
        <result column="pid" property="pid"/>
        <result column="createtime" property="createtime"/>
        <result column="updatetime" property="updatetime"/>
        <collection  property="children"  ofType="com.henu.entity.Category" column="id" select="selectByPid"/>
    </resultMap>
    <select id="selectByPid" resultType="com.henu.entity.Category">
        select * from category where pid=#{id} order by id desc
    </select>
    <select id="listByCondition" resultMap="categoryMap">
        select c1.* from category c1
        left join category c2
        on c1.id = c2.pid
<!--        select * from category-->
        <where>
            isnull(c1.pid)
            <if test="name !=null and name !=''">
                and (c1.name like concat('%',#{name},'%') or c2.name like concat('%',#{name},'%'))
            </if>
        </where>
       group by c1.id
        order by c1.id desc
    </select>
    <insert id="save">
        insert into category(name,remark,pid)
        value(#{name},#{remark},#{pid})
    </insert>
    <update id="update">
        update category set name=#{name},remark=#{remark},updatetime= #{updatetime} where id=#{id}
    </update>
    <delete id="deleteById">
        delete from category where id=#{id}
    </delete>
    <select id="list" resultType="com.henu.entity.Category">
        select * from category order by id desc
    </select>
    <select id="getById" resultType="com.henu.entity.Category">
        select *from category where id=#{id}
    </select>
    <select id="getByUsername" resultType="com.henu.entity.Category">
        select * from category where name=#{name}
    </select>
</mapper>